<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RegistrosAtenciones">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.seleccioninstitucion.SeleccionInstitucionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.pacientes.PacientesActivity"
            android:exported="false" />

        <activity
            android:name=".ui.altapaciente.AltaPacienteActivity"
            android:exported="false" />

        <activity
            android:name=".ui.fichapaciente.FichaPacienteActivity"
            android:exported="false" />

        <activity
            android:name=".ui.registraratencion.RegistrarAtencionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.sincronizacion.SincronizacionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false" />

    </application>

</manifest>